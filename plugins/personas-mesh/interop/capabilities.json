{
  "schemaVersion": 3,
  "component": "personas-mesh",
  "canonicalRuntime": "claude-code",
  "portableSkills": ["mesh-doctor", "setup", "status"],
  "executables": [
    "bin/install-launchers",
    "bin/mirror-all",
    "bin/render-config",
    "bin/sync-all",
    "bin/sync-persona",
    "bin/sync-user-all",
    "bin/sync-user-persona"
  ],
  "knowledgeEdges": [
    {"kind": "import", "target": "Personas/Fleet/Persona Mesh", "consumers": ["setup", "mesh-doctor", "status"]}
  ],
  "executableDependencies": [
    {
      "id": "onepassword",
      "kind": "runtime-capability",
      "immutableSourcePin": "official 1Password CLI and MCP",
      "capabilities": ["secret reference resolution without value disclosure"],
      "required": "conditional"
    },
    {
      "id": "git-rsync-systemd",
      "kind": "runtime-capability",
      "immutableSourcePin": "host-managed executables",
      "capabilities": ["repository sync", "local user-data reconciliation", "scheduled launchers"],
      "required": true
    }
  ],
  "credentialBoundary": "Profiles contain 1Password references and nonsecret paths only. Resolved values never enter arguments, logs, or source.",
  "runtimes": {
    "claude-code": {"status": "native", "skills": true, "hooks": true, "scripts": true},
    "codex": {
      "status": "generated",
      "skills": true,
      "scripts": true,
      "hooks": "unsupported",
      "reason": "Portable operations and scripts are generated; Claude SessionStart/Stop hooks have no required Codex equivalent."
    },
    "gemini-cli": {"status": "unsupported", "reason": "No adapter is authored."},
    "kimi-code": {"status": "unsupported", "reason": "No adapter is authored."}
  }
}
