{
  "description": "Persona framework drift notice using the installed plugin root",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/framework-version.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
